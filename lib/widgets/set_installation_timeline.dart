import 'package:flutter/material.dart';

class SetInstallationTimeline extends StatelessWidget {
  const SetInstallationTimeline({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}